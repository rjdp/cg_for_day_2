apiVersion: v1
kind: Service
metadata:
  name: {{ include "chaosgenius.fullname" . }}
  labels:
    {{- include "chaosgenius.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chaosgenius.selectorLabels" . | nindent 4 }}
