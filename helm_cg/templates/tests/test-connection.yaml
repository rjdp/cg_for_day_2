apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chaosgenius.fullname" . }}-test-connection"
  labels:
    {{- include "chaosgenius.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chaosgenius.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
